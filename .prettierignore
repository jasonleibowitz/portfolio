dist/
.astro/
node_modules/
pnpm-lock.yaml
public/

# Handlebars templates -- prettier reformats {{ expr }} into { { expr } },
# which silently breaks `pnpm plop`.
plop-templates/

# Generated by the impeccable design hook; rewritten on every UI edit.
.impeccable/hook.cache.json

# Critique snapshots are generated archives, not hand-edited source.
.impeccable/critique/

# The data of an agent, and the git worktrees in it. `pnpm format` changes the
# files that it reads, thus without this line it changes files on a different
# branch.
.claude/
