dist/
.astro/
node_modules/
pnpm-lock.yaml
public/

# Handlebars templates -- prettier reformats {{ expr }} into { { expr } },
# which silently breaks `pnpm plop`.
plop-templates/

# Generated by the impeccable design hook; rewritten on every UI edit.
.impeccable/hook.cache.json

# Critique snapshots are generated archives, not hand-edited source.
.impeccable/critique/

# Agent state, and the git worktrees under it. `pnpm format` writes, so without
# this line it reformats files in another branch's checkout.
.claude/
