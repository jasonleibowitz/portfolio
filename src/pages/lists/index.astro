---
import ContentListLayout from '@layouts/ContentListLayout.astro';
import { getPublished } from '../../lib/content';

const lists = await getPublished('lists');
---

<ContentListLayout pageTitle="Listicles" items={lists} baseUrl="/lists" />
