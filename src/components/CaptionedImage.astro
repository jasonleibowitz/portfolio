---
const { alt, src, caption } = Astro.props;
---

<figure class="flex flex-col items-center">
  <img alt={alt ?? caption} src={src} />
  {caption && <p class="text-sm pt-2 text-center">{caption}</p>}
</figure>
